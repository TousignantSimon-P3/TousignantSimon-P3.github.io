// Fichier : Livre.hpp
#pragma once

#include <string>

// La seule responsabilité de ce fichier est de définir la struct Livre.
struct Livre {
    std::string titre;
    std::string auteur;
    int annee_publication;
};
